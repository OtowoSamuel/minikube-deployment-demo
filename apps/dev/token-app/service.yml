apiVersion: v1
kind: Service
metadata:
  name: token-app
  namespace: token-app
spec:
  selector:
    app: token-app
  ports:
    - port: 80          # External (what other services see)
      targetPort: 8080  # Internal (what container listens on)
  type: ClusterIP
